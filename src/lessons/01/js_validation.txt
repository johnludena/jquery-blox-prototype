try {
  if (typeof purpleButton !== "undefined") {
    console.log("IF: purpleButton is truthy!");
    window.top.postMessage({
        internalSignal: true,
        error: false,
        message: "Hello world!",
      },
      window.location.origin
    );
  } else {
    console.log("ELSE: purpleButton is not defined");
    console.log(purpleButton);
  }
} catch (error) {
  console.log(error);
}
