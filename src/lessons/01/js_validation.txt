describe('Array tests', () => {
  
  it('has three items', () => {
    expect(fruits).toHaveLength(3);
    testStatus = true;
  });

  it('is defined', () => {
    expect(fruits).toBeDefined();
    testStatus = true;
  });
  
});
