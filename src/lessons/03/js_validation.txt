describe('Lesson 2 Tests', () => {
  
  it('is defined', () => {
    expect(havingFunYet).toBeDefined();
    testStatus = true;
  });

  it('is a boolean', () => {
    expect(havingFunYet).toBe(true);
    testStatus = true;
  });

});
