describe('Lesson 4 Test', () => {
  
  it('variable "fruits" is defined', () => {
    expect(fruits).toBeDefined();
    testStatus = true;
  });

  // it('variable "fruits" is an array', () => {
  //   expect(fruits).toBe(true);
  //   testStatus = true;
  // });

  // it('variable "fruits" is an array with length of 5', () => {
  //   expect(fruits).toBe(true);
  //   testStatus = true;
  // });

  // it('variable "numeroTres" is defined', () => {
  //   expect(numeroTres).toBeDefined();
  //   testStatus = true;
  // });

});
