describe('Lesson 2 Tests', () => {
  
  it('is defined', () => {
    expect(myAge).toBeDefined();
    testStatus = true;
  });

  it('is an integer', () => {
    expect(myAge).toBe(36);
    testStatus = true;
  });

});
