try {
    let validatedStatus = null;
    let lessonMessage = null;
    let errorMessage = ''
  if (typeof blueDiv !== "undefined") {
    console.log("CORRECT: blueDiv is truthy!");
    validatedStatus = true;
    lessonMessage = "You just passed lesson 1!"
    
  } else {
    console.log("INCORRECT: blueDiv is not defined");
    validatedStatus = false;
    lessonMessage = "Sorry, that's not the right answer"
  }

    // send data to app's outter window
    window.top.postMessage({
        internalSignal: true,
        validated: validatedStatus,
        message: lessonMessage
    },
    window.location.origin
    );
} catch (error) {
  console.log(error);
}

